- name: eb
  active: true
  path: ~/dev/eb
  git-remote-ref: origin/rework
  git-pull: git pull origin rework --ff-only
  test: make test && pytest -q --tb=short
  build: which pipx && pipx install . --force
  post-build: gb -I eb.yaml -a -w
  interval: 3600